name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: front-rfq/package-lock.json

      - name: Install dependencies
        working-directory: front-rfq
        run: npm ci --legacy-peer-deps

      - name: Lint
        working-directory: front-rfq
        run: npm run lint

  build:
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: front-rfq/package-lock.json

      - name: Install dependencies
        working-directory: front-rfq
        run: npm ci --legacy-peer-deps

      - name: TypeScript check
        working-directory: front-rfq
        run: npx tsc --noEmit

      - name: Build
        working-directory: front-rfq
        env:
          VITE_SUPABASE_URL: https://placeholder.supabase.co
          VITE_SUPABASE_ANON_KEY: placeholder-key-for-build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: front-rfq/dist
          retention-days: 7

  docker:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build \
            --build-arg VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }} \
            --build-arg VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }} \
            -t bideval:${{ github.sha }} \
            -t bideval:latest .

      # Uncomment when container registry is configured:
      # - name: Push to registry
      #   run: |
      #     echo ${{ secrets.REGISTRY_PASSWORD }} | docker login ${{ secrets.REGISTRY_URL }} -u ${{ secrets.REGISTRY_USERNAME }} --password-stdin
      #     docker tag bideval:${{ github.sha }} ${{ secrets.REGISTRY_URL }}/bideval:${{ github.sha }}
      #     docker tag bideval:latest ${{ secrets.REGISTRY_URL }}/bideval:latest
      #     docker push ${{ secrets.REGISTRY_URL }}/bideval:${{ github.sha }}
      #     docker push ${{ secrets.REGISTRY_URL }}/bideval:latest
